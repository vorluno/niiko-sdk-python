blank_issues_enabled: false
contact_links:
  - name: This client is GENERATED
    url: https://github.com/vorluno/niiko-sdk-python/issues
    about: >-
      Its code comes from the niiko action manifest, so a fix here would be lost on the next run. Report the
      problem in the repository above and it gets fixed at the source — which is also where the fix reaches
      the other client and the reference.
  - name: An API fault, not a client fault
    url: https://developers.niiko.org
    about: If the server answers something the reference does not explain, start there.
